name = "hepex_white_baseline"
description = "A minimal baseline agent for testing HEPEx green agent."
version = "0.1.0"

defaultInputModes = ["text"]
defaultOutputModes = ["text"]

[capabilities]
streaming = false

[[skills]]
id = "solve"
name = "Solve benchmark tasks"
description = """
A minimal baseline that runs the provided analysis entrypoint and checks outputs.
"""
tags = ["white agent", "baseline", "hepex"]
