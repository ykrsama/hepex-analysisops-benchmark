You are designing the L2 benchmark package for a high-energy physics scientific workflow task.

You are given a reference_analysis.md.

Your job is to generate:
1. l2_prompt.md
2. private_rubric_l2.yaml

--------------------------------
L2 Definition: Guided Autonomy
--------------------------------
- Physics goal is fixed
- Workflow is NOT explicitly specified
- Agent must reconstruct analysis

--------------------------------
CRITICAL DESIGN RULES
--------------------------------

1. DIMENSION BOUNDARY (STRICT)

Same as L1:
- execution → only outputs exist
- pipeline → workflow structure
- implementation → correctness
- reasoning → justification
- analysis → physics result quality
- validation → optional

--------------------------------

2. CHECK TYPE DEFINITIONS

- deterministic → numeric / structured
- structural → workflow presence
- heuristic → optional
- llm_judge → ONLY reasoning

--------------------------------

3. CONDITION FORMAT (MANDATORY)

ALL checks MUST be machine-readable.

Use structured fields:

condition:
  observable_detected: ...
  fit_result: ...
  workflow_components: ...
  dependencies: ...

--------------------------------

4. MACHINE-READABLE OUTPUTS

The prompt MUST require:

- submission_trace.json
- fit_result.json

--------------------------------

5. ANALYSIS (IMPORTANT IN L2)

- Do NOT require exact thresholds
- BUT require physics signal detection

Example:

condition:
  fit_result:
    mu:
      expected_range: [120, 130]
    significance:
      min: 2.0

--------------------------------

6. FLEXIBILITY

- DO NOT enforce exact cuts
- DO reward:
  - correct observable choice
  - reasonable selection strategy

--------------------------------

Output format EXACTLY:

=== FILE: l2_prompt.md ===
...

=== FILE: private_rubric_l2.yaml ===
...

--------------------------------

Here is the reference analysis:

{{REFERENCE_ANALYSIS_MD}}